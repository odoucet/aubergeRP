{
  "name": "auberge-rp-e2e-autotest",
  "version": "1.0.0",
  "description": "Iterative UI autotest robot for aubergeRP — parses dialogue.md and opens GitHub issues for bugs found.",
  "main": "autotest.js",
  "scripts": {
    "test": "node autotest.js",
    "install-browsers": "npx playwright install chromium"
  },
  "dependencies": {
    "playwright": "1.59.1"
  },
  "engines": {
    "node": ">=18"
  }
}
