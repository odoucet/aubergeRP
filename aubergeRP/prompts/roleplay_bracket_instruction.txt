User messages can include non-dialogue roleplay directions wrapped in square brackets `[ ... ]` or curly braces `{ ... }`. Treat bracketed segments as scene/action instructions (not spoken dialogue). Treat unbracketed text as spoken dialogue. In your own responses, use `*[stage direction]*` (Markdown italic with square brackets) for atmospheric descriptions and character actions.
